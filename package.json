{
  "name": "vscode-stripe",
  "displayName": "Stripe",
  "description": "Stripe for Visual Studio Code",
  "version": "0.0.1",
  "publisher": "stripe",
  "repository": "https://github.com/stripe/vscode-stripe",
  "engines": {
    "vscode": "^1.34.0"
  },
  "icon": "resources/icon_128.png",
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onDebugInitialConfigurations",
    "onDebug",
    "onView:stripeView",
    "onCommand:stripe.openCLI",
    "onCommand:stripe.openWebhooksListen",
    "onCommand:stripe.openLogsStreaming",
    "onCommand:stripe.openDashboardApikeys",
    "onCommand:stripe.openDashboardLogs",
    "onCommand:stripe.openDashboardEvents",
    "onCommand:stripe.openDashboardWebhooks"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "category": "Stripe",
        "command": "stripe.openWebhooksListen",
        "title": "Open Webhooks listening"
      },
      {
        "category": "Stripe",
        "command": "stripe.openLogsStreaming",
        "title": "Open Logs streaming"
      },
      {
        "category": "Stripe",
        "command": "stripe.openCLI",
        "title": "Open CLI"
      },
      {
        "category": "Stripe",
        "command": "stripe.openDashboardApikeys",
        "title": "Open Dashboard API keys"
      },
      {
        "category": "Stripe",
        "command": "stripe.openDashboardLogs",
        "title": "Open Dashboard Logs"
      },
      {
        "category": "Stripe",
        "command": "stripe.openDashboardEvents",
        "title": "Open Dashboard Events"
      },
      {
        "category": "Stripe",
        "command": "stripe.openDashboardWebhooks",
        "title": "Open Dashboard Webhooks"
      }
    ],
    "views": {
      "stripe": [
        {
          "id": "stripeView",
          "name": "Dashboard"
        },
        {
          "id": "stripeView2",
          "name": "Stripe"
        }
      ]
    },
    "debuggers": [
      {
        "type": "stripe",
        "label": "Stripe",
        "configurationSnippets": [
          {
            "label": "Stripe: Listen",
            "description": "Debug stripe webhooks",
            "body": {
              "type": "stripe",
              "request": "launch",
              "localUrl": "http://localhost:3000/stripe-events",
              "command": "listen"
            }
          }
        ],
        "configurationAttributes": {
          "launch": {
            "properties": {
              "localUrl": {
                "type": "string",
                "description": "Url for local http server",
                "default": "http://localhost:3000/stripe-events"
              },
              "command": {
                "type": "string",
                "description": "Command to execute",
                "default": "listen"
              }
            }
          }
        }
      }
    ],
    "viewsContainers": {
      "activitybar": [
        {
          "icon": "resources/icon.svg",
          "id": "stripe",
          "title": "Stripe"
        }
      ]
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "lint": "tslint -p ./",
    "watch": "tsc -watch -p ./"
  },
  "devDependencies": {
    "@types/node": "^10.14.17",
    "@types/vscode": "^1.34.0",
    "tslint": "^5.16.0",
    "typescript": "^3.5.1"
  }
}
